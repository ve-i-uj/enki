"""Подсистема сущностей.

Хранит данные о созданных, удалённых сущностях. Знает данные конфигурационного
файла kbengine.xml. Сериализут и десериализует сообщения сущностей (их RPC) в
KBEngine сообщения и обратно.

Сериализация / десериализация RPC делается на основе сгенерированных
сериализаторов по entity_defs и types.xml + данные из конфига kbengine.xml и
кол-ва сущностей.

Подсистема сущностей относится к сетевому слою (сеть, байты, декодеры,
сериализаторы, сообщения). Её ответственность сериализовать / десериализовать
каждый вызов RPC-сущности. Побочных эффектов у подсистемы нет.
"""
